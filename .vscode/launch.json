{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "chrome",
      "request": "launch",
      "name": "Launch Chrome - Localhost",
      "url": "http://localhost:9002",
      "webRoot": "${workspaceFolder}"
    }
  ]
}
